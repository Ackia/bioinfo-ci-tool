name: Build & Push samtools
on:
  push:
    branches: [main]
  workflow_dispatch:
  repository_dispatch:
    types: [docker-build]
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        version: [1.21]
        arch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./samtools
          file: ./samtools/Dockerfile
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: |
            ghcr.io/ackia/samtools:${{ matrix.version }}
            ghcr.io/ackia/samtools:latest
          build-args: |
            VERSION=${{ matrix.version }}
